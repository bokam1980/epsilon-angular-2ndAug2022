//create two functions to add two numbers and subtract two numbers
//ES6 format to export
export function add(a: number, b: number): number {
    return (a + b);
}
export const subtract = (a: number, b: number): number => {
    return (a - b);
}

// module.exports = {
//     // addFn: add,
//     // subtractFn: subtract
//     add, //ad:add
//     subtract //subtract:subtract
// }
//any